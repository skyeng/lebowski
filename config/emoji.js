module.exports = {
  /**
   * Bot listens for this reaction and writes the time when the reaction is set to the Google Sheet (`acknowledged_at`)
   */
  workInProgress: "workinprogress",

  /**
   * This emoji is included in bot's weekly/daily reports so that other custom bots you might have can ignore its messages
   */
  mute: "mute",
};
